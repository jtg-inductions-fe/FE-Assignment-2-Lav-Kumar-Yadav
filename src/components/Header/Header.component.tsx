import { AppBar } from '@mui/material';

export const Header = () => (
    <>
        <AppBar sx={{ bgcolor: 'primary.contrastText', color: 'black' }}>
            Header
        </AppBar>
    </>
);
