import { Skeleton } from '@mui/material';

/**
 * @component
 * @returns The Skeleton which can be render instead of LineCHart */
export const LineChartSkeleton = () => <Skeleton height={500} width="100%" />;
